{
  "name": "electricity-payment-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build -w @phedc/shared && npm run build -w @phedc/server && npm run build -w client",
    "start": "npm run start --workspace=apps/server"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
